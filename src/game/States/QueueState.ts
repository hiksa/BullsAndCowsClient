export class QueueState extends Phaser.State {
  public game: Phaser.Game;
  public background: Phaser.Sprite;

  public preload(): void {

  };

  public create(): void {
      // TODO: Find another player and transition to SecretNumberState
  };

  public update(): void {

  };

  private transitionToGameState(): void {
  };
};
